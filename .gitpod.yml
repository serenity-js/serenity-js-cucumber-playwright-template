tasks:
  - name: 'Install dependencies'
    before: |
      nvm install
      npm ci
      npx --yes playwright install --with-deps
      gp sync-done installation

  - name: 'Welcome'
    init: | 
      echo "Getting the environment ready..."
      gp sync-await installation
    command: |
      echo "Run 'npm test' to get started"
